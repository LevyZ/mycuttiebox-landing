# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Landing page for **MyCuttieBox** — a subscription box service. The project is in early setup; no source code exists yet.

## Setup

Once a framework is chosen, update this file with:
- Install command (e.g. `npm install`)
- Dev server command (e.g. `npm run dev`)
- Build command (e.g. `npm run build`)
- Lint/format commands
- How to run tests

## Notes

- Business and financial studies live in the parent directory (`../MyCuttieBox_Etude_Business.docx`, `../MyCuttieBox_Etude_Financiere.xlsx`) — consult them for product/brand context.
